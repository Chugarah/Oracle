version: '3.8'

services:
  downloader:
    # Uncomment and customize environment variables as needed
    environment:
      # URL: "https://www.youtube.com/watch?v=dQw4w9WgXcQ"  # Single URL to download
      # FORMAT: "bestvideo[height<=720]+bestaudio/best"     # Format specification
      # OUTPUT_TEMPLATE: "/downloads/%(uploader)s/%(title)s.%(ext)s"  # Output path template
      # AUTHENTICATION: "true"                              # Enable authentication
      # INPUT_FOLDER: "/downloads"                          # Where to save downloads
    
    # Uncomment to enable auto-restart
    # restart: unless-stopped
    
    # Uncomment for custom volume mappings
    # volumes:
    #   - ../my-downloads:/downloads                         # Custom download location
    #   - ../my-urls.txt:/app/urls.txt                       # Custom URL list
    #   - ../my-config:/app/script/downloader-conf           # Custom configuration 